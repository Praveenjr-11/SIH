{
  "name": "land-stack",
  "version": "1.0.0",
  "private": true,
  "description": "LAND STACK – Integrated GIS-based DPI for Land Governance (Decoupled Architecture)",
  "scripts": {
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build:backend": "npm --prefix backend run build",
    "build:frontend": "npm --prefix frontend run build",
    "gis:seed": "npm --prefix backend run gis:seed"
  }
}
